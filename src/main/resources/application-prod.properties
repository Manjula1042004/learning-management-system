# ===========================================
# PRODUCTION CONFIGURATION - FINAL WORKING VERSION
# ===========================================

# Server Configuration
server.port=${PORT:10000}
server.address=0.0.0.0
server.servlet.context-path=/
server.tomcat.uri-encoding=UTF-8

# Optimize Tomcat for low memory
server.tomcat.max-threads=5
server.tomcat.min-spare-threads=1
server.tomcat.max-connections=10
server.tomcat.accept-count=5

# Database Configuration - USING EXTERNAL HOSTNAME
spring.datasource.url=jdbc:postgresql://dpg-d6eksbh5pdvs73d0-a.render.com:5432/1msdb_3w4f
spring.datasource.username=lms_user
spring.datasource.password=cdJh9PX05MXnPvnb2P19AhyYeuFBI5Vu
spring.datasource.driver-class-name=org.postgresql.Driver

# Connection pool settings - Optimized for free tier
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.maximum-pool-size=2
spring.datasource.hikari.minimum-idle=1
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.max-lifetime=600000
spring.datasource.hikari.connection-test-query=SELECT 1

# JPA Configuration for PostgreSQL
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true
spring.jpa.properties.hibernate.temp.use_jdbc_metadata_defaults=false

# DISABLE ALL CACHES to save memory
spring.jpa.properties.hibernate.cache.use_second_level_cache=false
spring.jpa.properties.hibernate.cache.use_query_cache=false
spring.jpa.properties.hibernate.generate_statistics=false
spring.thymeleaf.cache=false
spring.resources.cache.cachecontrol.max-age=0

# Cloudinary Configuration (from environment variables)
cloudinary.cloud-name=${CLOUDINARY_CLOUD_NAME}
cloudinary.api-key=${CLOUDINARY_API_KEY}
cloudinary.api-secret=${CLOUDINARY_API_SECRET}

# PayPal Configuration (from environment variables)
paypal.client.id=${PAYPAL_CLIENT_ID}
paypal.client.secret=${PAYPAL_CLIENT_SECRET}
paypal.mode=${PAYPAL_MODE:sandbox}

# JWT Configuration (from environment variables)
jwt.secret=${JWT_SECRET}
jwt.expiration=86400000
jwt.refresh-expiration=604800000

# CORS Configuration - Allow Render domain
cors.allowed-origins=https://lms-application.onrender.com,http://localhost:8080
cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
cors.allowed-headers=*
cors.allow-credentials=true

# Thymeleaf Configuration - Minimal for production
spring.thymeleaf.cache=false
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
spring.thymeleaf.mode=HTML
spring.thymeleaf.encoding=UTF-8
spring.thymeleaf.check-template-location=true

# File Upload - Reduced limits for production
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB

# Logging Configuration - Show errors clearly
logging.level.com.lms=INFO
logging.level.org.springframework.web=INFO
logging.level.org.hibernate=WARN
logging.level.com.zaxxer.hikari=INFO
logging.level.org.postgresql=INFO

# DISABLE Swagger/OpenAPI in production
springdoc.api-docs.enabled=false
springdoc.swagger-ui.enabled=false

# Spring Security
spring.security.filter.dispatcher-types=REQUEST,ERROR,ASYNC