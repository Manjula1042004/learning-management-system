# ===========================================
# PRODUCTION CONFIGURATION - TEMPORARY TEST
# ===========================================

# Server
server.port=${PORT:10000}
server.address=0.0.0.0

# Database - HARDCODED FOR TESTING
spring.datasource.url=jdbc:postgresql://dpg-d6eksbh5pdvs73d0-a.render.com:5432/1msdb_3w4f
spring.datasource.username=lms_user
spring.datasource.password=cdJh9PX05MXnPvnb2Pi9AhgYeuFBI5Vu
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Cloudinary
cloudinary.cloud-name=${CLOUDINARY_CLOUD_NAME}
cloudinary.api-key=${CLOUDINARY_API_KEY}
cloudinary.api-secret=${CLOUDINARY_API_SECRET}

# PayPal
paypal.client.id=${PAYPAL_CLIENT_ID}
paypal.client.secret=${PAYPAL_CLIENT_SECRET}
paypal.mode=${PAYPAL_MODE}

# JWT
jwt.secret=${JWT_SECRET}
jwt.expiration=86400000

# Logging
logging.level.com.lms=INFO